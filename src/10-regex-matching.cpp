#include <iostream>

class Solution {
public:
    bool isMatch(std::string s, std::string p) {
    }
};

int main() {
    Solution s;
    auto result = s.isMatch("aa", "a");

    std::cout << "Result: " << result << std::endl;
    return 0;
}
